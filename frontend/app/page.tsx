'use client'

import { DisputePlayground } from '@/components/dispute-playground'

/**
 * App entry point.
 * The entire experience lives inside DisputePlayground — dispute simulation,
 * AI resolution, and the inline analyst investigation panel.
 */
export default function Page() {
  return <DisputePlayground />
}
